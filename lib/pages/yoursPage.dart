import 'package:flutter/material.dart';

class YoursPage extends StatelessWidget {
  const YoursPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(
        "Yours Page",
        style: TextStyle(fontSize: 50),
      ),
    );
  }
}
